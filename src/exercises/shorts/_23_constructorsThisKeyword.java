package exercises.shorts;

public class _23_constructorsThisKeyword {
    /**
     * CONSTRUCTORS
     *
     * - Special method that every class must have
     * - It's used when we create objects of a class
     * - We can use constructors to initialize tha objects instance variables
     * - Execution of constructor always depends on the object.
     *
     *
     * CREATING;
     * - Constructor is a special method that matches the name of the class and has no return type nor a specifier.
     *
     * No-Argument Constructor / Default Constructor
     * Parameterized Constructors
     */
}
