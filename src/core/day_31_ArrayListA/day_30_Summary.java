package core.day_31_ArrayListA;

/**
 * @author esalkan
 *
 */
public class day_30_Summary {

	/**
	 * @param args
	 */

	/**
	 * 
	 * WRAPPER CLASS
	 * 
	 * Why do we need them?
	 * - Wrapper classes are used to convert a primitive into object
	 * - Wrapper classes provide useful methods
	 * 
	 * Each primitive data types has a corresponsing wrapper class
	 * - byte->Byte
	 * - short->Short
	 * - int->Integer
	 * - long->Long
	 * - float->Float
	 * - double->Double
	 * - boolean->Boolean
	 * - char->Character
	 * 
	 * How to create wrapper class object?
	 * *** 3 way to create wrapper class...
	 * - WrapperName referanceName = new ClassName();
	 * - Integer num = new Integer(345);
	 * - Integer numm = new Integer("345);
	 * - Integer num = Integer.valueOf(345);
	 * 
	 * 
	 * Auto-Boxing & Un-Boxing
	 * - AUTO-BOXING :
	 * - Auto-Boxing is converting from primitive to Wrapper Class Object
	 * ** Integer num = 1234; 
	 * ** int num1 = 5;
	 * ** Integer num2 = num1;
	 * 
	 * - UN-BOXING
	 * - Un-Boxing is converting fromm Wrapper Class Object into Primitive
	 * ** Boolean b1 = new Boolean(true);
	 * ** boolean b2 = b1;
	 * 
	 * 
	 * 
	 * parseInt() -> returns a primitive value.
	 * ** int count = Integer.parseInt("234");
	 * 
	 * valueOf() -> returns a Wrapper Class Object
	 * ** Integer count2 = Integer.valueOf("53423");
	 * 
	 *  Wrapper Classes also come with usefull variables and methods.
	 *  - Variables : MIN_VALUE, MAX_VALUE
	 *  - Methods : valueOf(), parseInt(), isDigit(), isLetter(), isAlphabethic() 
	 *  
	 *  
	 *  
	 */

}
